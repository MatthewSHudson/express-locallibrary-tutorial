extends layout 

block content
  h1 #{title} #{bookinstance._id}

  p #[strong Are you sure you want to delete this copy?]
  form(method='POST')
    div.form-group
      input#instanceid.form-control(type='hidden' name='instanceid' value=bookinstance._id)
    button.btn.btn-primary(type='submit' ) Delete